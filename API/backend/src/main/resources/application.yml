spring:
  data:
    mongodb:
      uri: mongodb+srv://donambinh2002:passw0rd@news.s0vit.mongodb.net
      database: Accounts
  mail:
    host:
    port:
    username:
    password:
    properties:
      mail:
        smtp:
          auth:
          starttls:
            enable:
  config:
    import: optional:file:.env[.properties]
